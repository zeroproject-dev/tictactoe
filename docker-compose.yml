services:
  app:
    container_name: tictactoe
    image: tictactoe
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - 80
    networks:
      - proxy

networks:
  proxy:
    external: true
